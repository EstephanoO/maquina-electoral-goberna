{
  "name": "wspp-store-tester",
  "version": "10.5.2",
  "description": "Goberna Chrome Extension — modularized build",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "build:zip": "node build.js && node zip.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
