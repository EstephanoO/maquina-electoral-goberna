name: Ops Alerts Check

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ops-alerts:
    runs-on: ubuntu-latest
    steps:
      - name: Run alert checks on VPS
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.VPS_HOST }}
          username: deploy
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -euo pipefail
            cd /srv/app
            export OPS_ALERT_WEBHOOK_URL="${{ secrets.OPS_ALERT_WEBHOOK_URL }}"
            # Use backend port directly (bypass nginx HTTPS redirect)
            BACKEND_PORT=$(grep -oP '(?<=BACKEND_PORT=)\d+' /srv/app/.env 2>/dev/null || echo "3000")
            python3 scripts/ops/check_ops_alerts.py --api-base "http://127.0.0.1:${BACKEND_PORT}" --state-file /var/lib/nexus/ops-alert-state.json
