/**
 * Entry point — the router guard in _layout.tsx handles navigation.
 * This screen renders nothing; it's just a route target for the guard.
 */

import { View } from 'react-native';

export default function Index() {
  return <View />;
}
