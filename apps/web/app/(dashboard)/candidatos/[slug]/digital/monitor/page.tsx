"use client";

/**
 * /candidatos/[slug]/digital/monitor
 * Reusa el dashboard war-room de WhatsApp (6 phones + clasificaciones + SSE).
 */

import MonitorWaPage from "@/app/(dashboard)/candidatos/[slug]/monitor/page";

export default function DigitalMonitorTab() {
  return <MonitorWaPage />;
}
