"use client";

/**
 * /candidatos/[slug]/digital/whatsapp
 * Reusa la config de WhatsApp por candidato.
 */

import CandidatoWhatsAppPage from "@/app/(dashboard)/candidatos/[slug]/whatsapp/page";

export default function DigitalWhatsappTab() {
  return <CandidatoWhatsAppPage />;
}
