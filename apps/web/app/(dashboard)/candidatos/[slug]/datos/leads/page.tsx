"use client";

/**
 * /candidatos/[slug]/datos/leads
 * Reusa la lista de leads TestFlight (admin-only).
 */

import LeadsPage from "@/app/(dashboard)/leads/page";

export default function DatosLeadsTab() {
  return <LeadsPage />;
}
